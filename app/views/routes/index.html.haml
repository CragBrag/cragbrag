- model_class = Route
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize.titleize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:gym_id)
      %th= model_class.human_attribute_name(:active)
      %th= model_class.human_attribute_name(:name)
      %th= model_class.human_attribute_name(:grade)
      %th= model_class.human_attribute_name(:created_at)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @routes.each do |route|
      %tr
        %td= link_to route.id, gym_route_path(route.gym, route)
        %td= route.gym_id
        %td= route.active
        %td= route.name
        %td= route.grade
        %td=l route.created_at
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_gym_route_path(route.gym, route), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), gym_route_path(route.gym, route), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

= link_to t('.new', :default => t("helpers.links.new")), new_gym_route_path, :class => 'btn btn-primary'
