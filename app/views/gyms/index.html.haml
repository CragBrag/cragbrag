%h1 Gyms

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Problems
      %th
  %tbody
    - @gyms.each do |gym|
      %tr
        %td= link_to gym.name, gym_path(gym)
        %td= gym.problems.count
        %td
          %span.pull-right
            = link_to edit_gym_path(gym), class: 'btn btn-mini' do
              %i.icon-edit
